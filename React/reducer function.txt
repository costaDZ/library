function reducer(state, action) {
    switch (action.type) {
        case 'LOAD':
            return .......
        case 
            return item;
        default:
            return state;
    }
}

export default reducer